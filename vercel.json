{
  "version": 2,
  "builds": [
    {
      "src": "vite.config.js",
      "use": "@vercel/static-build"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "http://localhost:8000/$1" },
    { "src": "/(.*)", "dest": "/build/$1" }
  ]
}
